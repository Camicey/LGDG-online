A faire : 
- Un état de sauvegarde pour le gamemanager pour restaurer pour les joueurs qui buggent. => avoir une liste 
- Se retourne quand il y a un soucis de lien
- Tout se "supprime" en fin de partie ; -; 
- J'essaie de piocher pour l'autre joueur mais j'ai pas l'authorité de le faire 
- Ok, tout le monde gagne si yen a un qui gagne, voir comment choisir un côté ou l'autre
- Apprendre a faire des signaux ? Plug-in voir Marie et Temmie

En train de faire : 
Encore un bug d'échange de cartes XD du côté client
toujours hein mais je change le système de terrain et je les initialise
PMEncours

Problèmes : 
Les cartes depop ;-; quand je quitte et revient > J'ai rajouté un bouton pour initialiser les cartes dans la pioche
> Problème 2 : Je ne peux pas créer de pioche côté client. (Il parrait que le serveur est pas prêt) 
> Problème 3 : Ca fout en l'air le concept de gagner (Il faudrait pouvoir clear avant de créer à nouveau même s'il y a répétition)


Choses apprises : 

On ne peut pas appeler un RPC dans un RPC

[SyncVar(hook = nameof(OnPMChanged))]
    public int PMEnCours = 20;
Ca c'est un hook, il s'active dès que la variable est changée, ca aurait été plus facile avec a peu près tout HEIN

Bout a replacer plus tard : 

 /*Vérifier le nombre de joueur avant de permettre de piocher, à faire plus tard.
        private void Update()
        {
            if (NetworkServer.connections.Count == 2) { }
        }
    */

MoveTowards est une méthode de Unity qui permet de déplacer un objet d'une position actuelle vers une position cible à une vitesse constante, sans interpolation.
La syntaxe est simple : Vector3. MoveTowards(current, target, maxDistanceDelta).